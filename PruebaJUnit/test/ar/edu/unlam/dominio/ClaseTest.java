package ar.edu.unlam.dominio;

public class ClaseTest {
	
	public void pasosParaDesarrollarConTDD () {
			
			/*
			 * Escribimos una prueba (generar clases, metodos y elementos necesarios
			 * para que compile). Ejecutar la prueba y debe dar ROJO (la prueba no pasa)
			 * Lo cual indicaría que NO HACE lo que debería hacer
			 * 
			 * Agregamos el código productivo JUSTO Y NECESARIO para que la prueba funcione
			 * (que dé en color VERDE). No escribir de más ni por las dudas
			 * 
			 * Re organizamos el código (refactorizar) si es posible o si es util
			 * 
			 * Importante: las pruebas ejecutan metodos, probamos metodos
			 */
		}
		
		public void contenidoDeUnaPrueba() {
			
			/*
			 * Preparacion --> los elementos necesarios para garantizar que la prueba sea efectiva
			 * Ejemplo, si tengo que hacer una suma, NECESITO que estén esos números, sin ellos no
			 * podria sumar. Todos los datos necesarios para una prueba deben estar presentes
			 * 
			 * Ejecucion --> buscamos probar un metodo, un procedimiento. En este ejemplo ficticio
			 * deberiamos ejecutar sumar(2, 3)
			 * 
			 * Verificacion --> la etapa en la que se valida que realmente se llego al resultado
			 * que se esperaba, no que solo se haya llegado a un resultado cualquiera
			 * En otras palabras buscamos llegar al resultado que da VERDE
			 */
		}
		
		public void nomenclaturaDePruebas() {
			
			/*
			 * Es muy importante usar nombres representativos para
			 * cada una de las pruebas que hacemos a fin de poder
			 * seguir correctamente lo que hace cada una de ellas
			 * 
			 * Dado que (given): aclara el contexto de la prueba
			 * Cuando (when): aclara que es lo que se esta ejecutando
			 * Entonces (then): aclara el resultado esperado
			 */
		}
}

